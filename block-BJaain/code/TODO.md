### Read the following lines to solve the assignment

- You will find the `index.js` file in the `code` folder
- In `index.js` you will find the instruction
- Write the output where ever `Output` is needed
